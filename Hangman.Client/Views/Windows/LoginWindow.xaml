<Window x:Class="Hangman.Client.Views.Windows.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Inicio de Sesión" Height="1080" Width="1920"
        WindowStartupLocation="CenterScreen" WindowState="Maximized">

    <Grid>
        <Grid.Background>
            <SolidColorBrush Color="Red"/>
        </Grid.Background>

        <Border Width="1100" Height="650" Background="#E6F2E3D5" CornerRadius="20"
                VerticalAlignment="Center" HorizontalAlignment="Center">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Margin="40,40,40,20" Background="#E5C39C" CornerRadius="15" Height="110" BorderBrush="#C79F73" BorderThickness="2">
                    <TextBlock Text="EL AHORCADO" FontSize="70" FontWeight="Bold" 
                               Foreground="#3D2314" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>

                <Grid Grid.Row="1" Margin="80,30,80,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Correo electrónico" FontSize="24" VerticalAlignment="Center" Margin="0,0,30,30"/>

                    <TextBox Grid.Row="0" Grid.Column="1" Margin="0,0,0,30"/>

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Contraseña" FontSize="24" VerticalAlignment="Center" Margin="0,0,30,10"/>

                    <PasswordBox Grid.Row="1" Grid.Column="1" Margin="0,0,0,10"/>

                    <CheckBox Grid.Row="2" Grid.Column="1" Content="Mostrar Contraseñas" Margin="0,5,0,0"/>
                </Grid>

                <Grid Grid.Row="2" Margin="40,10,40,30">

                    <Button Content="Iniciar Sesión" Width="350" Height="65" 
                            Click="Button_Click" 
                            Style="{StaticResource BotonMaderaStyle}"/>

                    <Button Width="65" Height="65" HorizontalAlignment="Right" VerticalAlignment="Bottom"
                            Click="Button_Click_1" 
                            Style="{StaticResource BotonMaderaStyle}">
                        <TextBlock Text="⚙" FontSize="40" Foreground="#3D2314" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,-5,0,0"/>
                    </Button>

                </Grid>

                <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,30">
                    <TextBlock Text="Crear cuenta" FontSize="20" Foreground="#3D2314" Cursor="Hand" TextDecorations="Underline"/>
                </StackPanel>

            </Grid>
        </Border>
    </Grid>
</Window>